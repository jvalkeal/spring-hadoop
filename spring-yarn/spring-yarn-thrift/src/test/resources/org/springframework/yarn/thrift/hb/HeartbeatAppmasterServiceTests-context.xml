<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
			http://www.springframework.org/schema/beans/spring-beans.xsd
			http://www.springframework.org/schema/task
			http://www.springframework.org/schema/task/spring-task.xsd">

	<task:executor id="taskExecutor" pool-size="5"/>
	<task:scheduler id="taskScheduler" pool-size="5"/>

	<!-- We can't use random ports in tests, so use below trick to get one available -->
	<bean id="tcpIpUtils" class="org.springframework.yarn.thrift.util.SocketUtils" />

	<bean id="serverSocketPort" class="java.lang.Integer" >
		<constructor-arg index="0" value="#{tcpIpUtils.findAvailableServerSocket(7400)}"/>
	</bean>

	<bean id="clientServerSocketPort" class="java.lang.Integer" >
		<constructor-arg index="0" value="#{tcpIpUtils.findAvailableServerSocket(7500)}"/>
	</bean>

	<bean id="heartbeatAppmasterService" class="org.springframework.yarn.thrift.hb.HeartbeatAppmasterService">
		<property name="thriftServerPort" value="#{serverSocketPort.intValue()}" />
		<property name="warnTime" value="5000" />
		<property name="deadTime" value="10000" />
	</bean>

	<bean id="heartbeatAppmasterServiceClient" class="org.springframework.yarn.thrift.hb.HeartbeatAppmasterServiceClient">
		<property name="serverPort" value="#{serverSocketPort.intValue()}" />
		<property name="serverHost" value="localhost" />
		<property name="timeout" value="10000" />
		<property name="nodeId" value="1" />
		<property name="thriftServerPort" value="#{clientServerSocketPort.intValue()}" />
	</bean>

</beans>
